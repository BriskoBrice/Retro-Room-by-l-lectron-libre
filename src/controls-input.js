function sendInput(index,value){if(!currentCore||!emulatorStarted)return;if(currentSystemId==='lowresnx'){sendLowResInput(index,value);return;}try{if(typeof window.simulate_input==='function'){window.simulate_input(0,Number(index),Number(value));}else if(window.EJS_emulator?.gameManager&&typeof window.EJS_emulator.gameManager.simulateInput==='function'){window.EJS_emulator.gameManager.simulateInput(0,Number(index),Number(value));}}catch(_){}}
function bindInputButton(btn){if(btn.dataset.bound==='1')return;btn.dataset.bound='1';let pressed=false;const down=e=>{e.preventDefault();if(pressed)return;pressed=true;btn.classList.add('active');try{btn.setPointerCapture(e.pointerId)}catch(_){}sendInput(btn.dataset.input,1);};const up=e=>{if(!pressed)return;e.preventDefault();pressed=false;btn.classList.remove('active');sendInput(btn.dataset.input,0);};btn.addEventListener('pointerdown',down,{passive:false});btn.addEventListener('pointerup',up,{passive:false});btn.addEventListener('pointercancel',up,{passive:false});btn.addEventListener('lostpointercapture',up,{passive:false});btn.addEventListener('contextmenu',e=>e.preventDefault());}
function addInputButton(container,label,input,className){const b=document.createElement('button');b.type='button';b.className=className;b.textContent=label;b.dataset.input=String(input);container.appendChild(b);bindInputButton(b);return b;}
function applyDirectionMode(){let map;if(directionMode==='analog')map=[19,17,16,18];else if(directionMode==='wsY')map=[12,9,10,11];else map=[4,6,7,5];dpadButtons.forEach((b,i)=>b.dataset.input=String(map[i]));app.dataset.dirMode=directionMode;const analog=document.getElementById('stickModeBtn');if(analog)analog.textContent=directionMode==='analog'?'STICK':'D-PAD';const ws=document.getElementById('wsPadBtn');if(ws)ws.textContent=directionMode==='wsY'?'PAD Y':'PAD X';}
function addModeToggle(id,label,onClick){const t=document.createElement('button');t.type='button';t.id=id;t.className='miniBtn modeToggle';t.textContent=label;t.addEventListener('click',e=>{e.preventDefault();onClick();});middleBtns.insertBefore(t,middleBtns.firstChild);return t;}
